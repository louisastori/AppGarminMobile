{
  "name": "@nouvelle-app/web",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "node --import tsx src/dev.ts",
    "build": "node --import tsx src/build.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nouvelle-app/api": "workspace:*",
    "@nouvelle-app/domain": "workspace:*",
    "@nouvelle-app/ui": "workspace:*"
  }
}
